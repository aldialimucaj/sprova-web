sudo: required
addons:
  chrome: stable

language: node_js
node_js:
  - "11"

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli

script:
  - ng build --prod --aot
  - npm test-coverage
  